notifications:
  webhooks:
    urls:
      http://23a3dc10.ngrok.io
    on_success: always
    on_failure: always
    on_start: always

#language: python
#script:
#- which gpg
#- gpg --list-secret-keys --keyid-format LONG
#- python -c 'import sys; print(sys.byteorder)'
#- gpg --import exported_key.gpg

